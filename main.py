# 1. RULE - if a tile has the same amount of hidden squares around it as unflaged
# 2. RULE - if a tile has the same amount of flags around it as the number on the square,
            # then all remaining hidden tiles around it are not bombs
# 3. RULE - implement logical rules - probability of each square being a bomb
